var a, b, c;

var a = 10, b = 20, c = 30;



var userName;
var _score;
var $price;
var totalMarks;
var camelCaseExample;


// var 123name;        // ❌ Cannot start with a number
// var user-name;      // ❌ Hyphen is not allowed (interpreted as minus)
// var var;            // ❌ 'var' is a reserved keyword
// var alert;          // ⚠️ Avoid using built-in function names (not technically illegal, but bad practice)
// var my variable;    // ❌ Spaces are not allowed


document.write("<h1>Rules for naming JS variables</h1>");
document.write("<p>Variable names can only contain <b>letters</b>, <b>numbers</b>, <b>underscores (_)</b> and <b>dollar signs ($)</b>. For example: <code>$my_1stVariable</code></p>");
document.write("<p>Variables must begin with a <b>letter</b>, <b>underscore (_)</b> or <b>dollar sign ($)</b>. For example: <code>$name</code>, <code>_name</code>, or <code>name</code></p>");
document.write("<p>Variable names are case <b>sensitive</b>.</p>");
document.write("<p>Variable names should not be JS <b>keywords</b>.</p>");